<project name="genParser" default="build">
  <!-- Generated by MPS -->
  <property name="build.dir" location="build" />
  <property name="build.mps.config.path" location="${build.dir}/config" />
  <property name="build.mps.system.path" location="${build.dir}/system" />
  <property name="build.tmp" location="${build.dir}/tmp/genParser" />
  <property name="build.layout" location="${build.dir}/artifacts/genParser" />
  
  <target name="assemble" depends="declare-mps-tasks">
    <mkdir dir="${build.layout}" />
  </target>
  
  <target name="buildDependents" />
  
  <target name="fetchDependencies" />
  
  <target name="build" depends="assemble, run-mps-code" />
  
  <target name="clean">
    <delete dir="${build.mps.config.path}" />
    <delete dir="${build.mps.system.path}" />
    <delete dir="${build.tmp}" />
    <delete dir="${build.layout}" />
  </target>
  
  <target name="run-mps-code" depends="assemble" />
  
  <target name="generate" depends="declare-mps-tasks" />
  
  <target name="declare-mps-tasks" />
  
  <target name="makeDependents" />
  
  <target name="generateParser">
    <java jar="external/java-cup-11b.jar" fork="true" dir="src/parser">
      <arg value="-interface" />
      <arg value="-parser" />
      <arg value="Parser" />
      <arg value="../petrinet.cup" />
    </java>
  </target>
</project>